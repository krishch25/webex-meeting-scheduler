# =================================================
# == Webex Scheduler Environment Configuration   ==
# =================================================
#
# This is an example file. Copy this to a new file named .env
# and fill in your own credentials.

# -- Server Configuration --
PORT=80
HTTPS_PORT=443

# -- JWT Configuration --
# Use a long, random, and secret string for production.
JWT_SECRET="YOUR_SUPER_SECRET_JWT_KEY_REPLACE_THIS"

# -- LDAP Configuration --
# Replace with your actual LDAP server details.
LDAP_SERVER="10.8.51.51"
LDAP_PORT=389
LDAP_BASE_DN_1="o=relianceada"
LDAP_BASE_DN_2="o=reliancegroup"

# -- Webex Service App Credentials --
# These are the credentials for your automated Webex application.
WEBEX_CLIENT_ID="YOUR_WEBEX_CLIENT_ID"
WEBEX_CLIENT_SECRET="YOUR_WEBEX_CLIENT_SECRET"
WEBEX_REFRESH_TOKEN="YOUR_WEBEX_REFRESH_TOKEN"
WEBEX_ACCESS_TOKEN="YOUR_INITIAL_WEBEX_ACCESS_TOKEN"

# -- Fixed Host Configuration --
# This is the single, centralized account that will host all meetings.
FIXED_HOST_EMAIL="your-service-account@example.com"
